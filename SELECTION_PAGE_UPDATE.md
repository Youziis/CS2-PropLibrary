# 选择道具页面更新说明

## 更新时间
2026-08-08

## 主要变更

### 筛选器变更
**原来**：
- 全部状态
- 未截图
- 已截图

**现在**：
- 全部状态
- 未拒绝（parsed + selected）
- 已拒绝（rejected）

---

## 功能说明

### 页面显示的道具状态
选择道具页面现在显示以下三种状态的道具：

1. **parsed**（已解析）
   - 刚从demo文件解析出来的道具
   - 待选择截图
   - 标识：✅ 正常

2. **selected**（已选择）
   - 已勾选但还未截图的道具
   - 标识：✅ 正常

3. **rejected**（已拒绝）⭐ 新增
   - 在审核页面被拒绝的道具
   - 可以重新选择并截图
   - 标识：❌ 已拒绝
   - 视觉效果：红色边框 + 淡红色背景
   - 提示：🔄 可重新选择截图

---

## 已拒绝道具的工作流程

### 场景：道具被拒绝后想重新截图

```
1. 在"审核道具"页面点击【拒绝】
   ↓
2. 道具状态变为 rejected
   ↓
3. 截图文件被删除
   ↓
4. 前往"选择道具"页面
   ↓
5. 筛选器选择"已拒绝"
   ↓
6. 看到所有被拒绝的道具（红色边框）
   ↓
7. 勾选需要重新截图的道具
   ↓
8. 点击"保存选择并准备截图"
   ↓
9. 道具状态变为 selected
   ↓
10. 运行截图工具重新截图
   ↓
11. 道具状态变为 screenshotted
   ↓
12. 在"审核道具"页面重新审核
```

---

## 视觉变化

### 正常道具
```
┌─────────────────────────────┐
│ 🔵 烟雾弹  CT  ✅ 正常      │
│ 地图: de_mirage              │
│ 玩家: s1mple                 │
│ ...                          │
└─────────────────────────────┘
```

### 已拒绝道具
```
┌─────────────────────────────┐ ← 红色边框
│ 🔵 烟雾弹  CT  ❌ 已拒绝    │
│ 地图: de_mirage              │
│ 玩家: s1mple                 │
│ 🔄 可重新选择截图           │ ← 特殊提示
│ ...                          │
└─────────────────────────────┘
  ↑ 淡红色背景
```

---

## 统计卡片变化

**原来**：
```
共 100 个，已截图 20 个
--------------------
类型统计：未截图: XX
```

**现在**：
```
共 100 个，已拒绝 15 个
--------------------
类型统计：已拒绝: XX
```

---

## 筛选器逻辑

### 全部状态
显示所有 parsed + selected + rejected 的道具

### 未拒绝
只显示 parsed + selected 的道具（正常流程的道具）

### 已拒绝
只显示 rejected 的道具（被拒绝后可重新选择）

---

## API 变更

### `/api/all_pending` (GET)
**变更前**：
```python
返回 parsed + selected 状态的道具
```

**变更后**：
```python
返回 parsed + selected + rejected 状态的道具
```

---

## 测试步骤

### 测试1：拒绝道具后查看
1. 在"审核道具"页面拒绝一个道具
2. 进入"选择道具"页面
3. 选择筛选器"已拒绝"
4. **验证**：看到被拒绝的道具，带有红色边框和 ❌ 标识

### 测试2：重新选择截图
1. 勾选一个已拒绝的道具
2. 点击"保存选择并准备截图"
3. 运行截图工具
4. **验证**：道具重新进入"审核道具"页面

### 测试3：筛选功能
1. 进入"选择道具"页面
2. 测试三个筛选器：
   - 全部状态：显示所有道具
   - 未拒绝：不显示红色边框的道具
   - 已拒绝：只显示红色边框的道具

---

## 状态流转图

```
parsed (已解析)
  ↓ 选择
selected (已选择)
  ↓ 截图
screenshotted (待审核)
  ↓ 审核
  ├─ 批准 → approved
  ├─ 拒绝 → rejected ←┐
  └─ 删除 → 永久删除   │
              ↓         │
         重新选择 ──────┘
         (回到选择页面)
```

---

## 相关文件

### 修改的文件：
1. `backend/routes/utility.py` - API返回rejected状态
2. `client/admin/script.js` - 筛选逻辑和UI
3. `client/admin/style.css` - 已拒绝道具样式

---

## 🚨 重要：需要重启

### 1. 重启后端服务器
```bash
cd backend
# 按 Ctrl+C 停止
python app.py
```

### 2. 刷新浏览器
按 `Ctrl+Shift+R` 强制刷新

---

## 完成 ✅

所有修改已完成！现在已拒绝的道具可以：
- ✅ 在选择页面看到（红色边框标识）
- ✅ 使用筛选器"已拒绝"筛选
- ✅ 重新勾选并截图
- ✅ 重新进入审核流程

这样就实现了完整的道具二次处理流程！
